# Production Environment Variables
# This file contains secrets and should NEVER be committed to version control
# Set these as Vercel environment variables instead

# Database - Neon PostgreSQL
# Connection string format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL=REPLACE_WITH_NEON_DATABASE_URL

# LLM Configuration - Google Gemini
GEMINI_API_KEY=REPLACE_WITH_YOUR_GEMINI_API_KEY

# Authentication Secrets (Generate new values for production)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BETTER_AUTH_SECRET=REPLACE_WITH_32_CHAR_RANDOM_SECRET
JWT_SECRET=REPLACE_WITH_32_CHAR_RANDOM_SECRET
JWT_EXPIRES_IN=24h

# Application URLs
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app
AUTH_BASE_URL=https://your-domain.vercel.app
ALLOWED_ORIGINS=https://your-domain.vercel.app

# OAuth Providers (Optional)
GOOGLE_CLIENT_ID=REPLACE_IF_USING_GOOGLE_AUTH
GOOGLE_CLIENT_SECRET=REPLACE_IF_USING_GOOGLE_AUTH

# Email Configuration (Optional for password reset)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@your-domain.com
