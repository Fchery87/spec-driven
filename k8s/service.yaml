apiVersion: v1
kind: Service
metadata:
  name: spec-driven
  namespace: spec-driven
  labels:
    app: spec-driven
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: spec-driven
